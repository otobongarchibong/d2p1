# Required — from https://console.anthropic.com (API Keys)
ANTHROPIC_API_KEY=sk-ant-...

# Required — Ahrefs: Account settings -> API keys
AHREFS_API_KEY=...

# Optional — if set, the app asks for this passcode before use
APP_PASSCODE=

# Optional — port to listen on (default 8787)
PORT=8787
